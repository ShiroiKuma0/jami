diff -ruN base/src/connectionmanager.cpp work/src/connectionmanager.cpp
--- a/src/connectionmanager.cpp	2026-08-08 12:50:05.603168430 +0200
+++ b/src/connectionmanager.cpp	2026-08-08 12:50:05.610991189 +0200
@@ -1194,6 +1194,10 @@
                 return;
             if (logger)
                 logger->warn("[channel {}] Timeout waiting for channel reply", name);
+            // SK-ICEDIAG: this kills the ENTIRE peer connection, not just the channel, and Jami
+            // arms it at 3 s for every "sip" request (jamiaccount.cpp:4216). Prime suspect for the
+            // 45 s median connection life. dhtnet's own warn() above is silent in release builds.
+            SK_CM("channel-timeout-kill channel=%s", name.c_str());
             if (auto sock = wSock.lock())
                 sock->shutdown();
         });
diff -ruN base/src/multiplexed_socket.cpp work/src/multiplexed_socket.cpp
--- a/src/multiplexed_socket.cpp	2026-08-06 22:27:44.000000000 +0200
+++ b/src/multiplexed_socket.cpp	2026-08-08 12:50:23.611783206 +0200
@@ -50,6 +50,18 @@
     MSGPACK_DEFINE_MAP(v)
 };
 
+// SK-ICEDIAG (2026-07-30): WHICH path tore this peer connection down. Four distinct paths all end
+// in Impl::shutdown() with a default-constructed error_code, so the existing "ec=0 (clean)" line
+// cannot tell a polite peer close from our own 3-second SIP channel deadline destroying a healthy
+// multiplexed socket. Established: ~64 peer sessions born and died per 32 min (one every ~30 s),
+// and every death fans out ~8 immediate swarm reconnects with no backoff. This names the killer.
+#ifdef __ANDROID__
+#include <android/log.h>
+#define SK_MS(...) __android_log_print(ANDROID_LOG_ERROR, "SK-ICEDIAG", __VA_ARGS__)
+#else
+#define SK_MS(...) do {} while (0)
+#endif
+
 namespace dhtnet {
 
 using clock = std::chrono::steady_clock;
@@ -92,7 +104,7 @@
         if (!isShutdown_) {
             if (endpoint)
                 endpoint->setOnStateChange({});
-            shutdown();
+            shutdown({}, "join-last-reference");
         } else {
             std::error_code ec;
             {
@@ -121,11 +133,12 @@
         }
     }
 
-    void shutdown(std::error_code ec = {})
+    void shutdown(std::error_code ec = {}, const char* skWhy = "unspecified")
     {
         std::unique_lock lk {stateMutex};
         if (isShutdown_.exchange(true))
             return;
+        SK_MS("mxshutdown why=%s ec=%d", skWhy, ec.value());
         shutdownEc_ = std::move(ec);
         stop.store(true);
         beaconTimer_.cancel();
@@ -148,14 +161,14 @@
     // shutdown() runs a user callback on every channel, so it must not run on a thread that is
     // already inside one of them - which is exactly the case for a write failing from a receive
     // callback. Mark the socket unusable now and tear it down elsewhere.
-    void shutdownAsync(std::error_code ec)
+    void shutdownAsync(std::error_code ec, const char* skWhy = "unspecified")
     {
         stop.store(true);
         if (shutdownPosted_.exchange(true))
             return;
-        dht::ThreadPool::io().run([w = parent_.weak_from_this(), ec] {
+        dht::ThreadPool::io().run([w = parent_.weak_from_this(), ec, skWhy] {
             if (auto shared = w.lock())
-                shared->pimpl_->shutdown(ec);
+                shared->pimpl_->shutdown(ec, skWhy);
         });
     }
 
@@ -274,7 +287,7 @@
         if (state == tls::TlsSessionState::SHUTDOWN && !this_.isShutdown_) {
             if (this_.logger_)
                 this_.logger_->debug("[device {}] Tls endpoint is down, shutdown multiplexed socket", this_.deviceId);
-            this_.shutdown();
+            this_.shutdown({}, "tls-state-shutdown");
         }
     });
     sendVersion();
@@ -292,12 +305,12 @@
         if (size < 0) {
             if (ec && logger_)
                 logger_->error("[device {}] Read error detected: {}", deviceId, ec.message());
-            shutdown(ec);
+            shutdown(ec, "read-error");
             break;
         }
         if (size == 0) {
             // We can close the socket
-            shutdown();
+            shutdown({}, "peer-eof");
             break;
         }
 
@@ -377,7 +390,7 @@
                 if (shared->pimpl_->logger_)
                     shared->pimpl_->logger_->error("[device {}] Beacon doesn't get any response. Stopping socket",
                                                    shared->pimpl_->deviceId);
-                shared->pimpl_->shutdown(std::make_error_code(std::errc::timed_out));
+                shared->pimpl_->shutdown(std::make_error_code(std::errc::timed_out), "beacon-timeout");
             }
         }
     });
@@ -762,7 +775,7 @@
     if (ec) {
         if (pimpl_->logger_)
             pimpl_->logger_->error("[device {}] Error when writing on socket: {:s}", pimpl_->deviceId, ec.message());
-        pimpl_->shutdownAsync(ec);
+        pimpl_->shutdownAsync(ec, "write-error");
     }
     return res;
 }
@@ -779,7 +792,12 @@
 void
 MultiplexedSocket::shutdown()
 {
-    pimpl_->shutdown();
+    // Callers live outside this file (connectionmanager's 3 s channelTimeout, ConnectionInfo::
+    // shutdown(), ~ConnectionInfo->join(), closeConnectionsWith). Record the return address rather
+    // than threading a reason through the public API — symbolise it offline with llvm-symbolizer
+    // against the unstripped libjami-core-jni.so.
+    pimpl_->shutdown({}, "public-shutdown");
+    SK_MS("mxshutdown caller_ra=%p", __builtin_return_address(0));
 }
 
 bool
