--- a/src/ice_transport.cpp
+++ b/src/ice_transport.cpp
@@ -460,7 +460,11 @@
 //==============================================================================
 
 IceTransport::Impl::Impl(std::string name, const std::shared_ptr<Logger>& logger, std::unique_ptr<std::mt19937_64>&& rng)
-    : logger_(logger ? (name.empty() ? logger : logger->createChild(std::move(name))) : nullptr)
+    // SK-ICEDIAG: copy, do NOT std::move. createChild() takes its tag BY VALUE, so moving here
+    // leaves `name` empty before the constructor body ever runs — which silently blanked the
+    // peer= field on every create line this diag has ever emitted. One string copy per transport
+    // (a few per minute) buys the transport-id -> device linkage the census needs.
+    : logger_(logger ? (name.empty() ? logger : logger->createChild(name)) : nullptr)
     , rng_(rng ? std::move(rng)
                : std::make_unique<std::mt19937_64>(dht::crypto::getSeededRandomEngine<std::mt19937_64>()))
 {
@@ -468,15 +472,33 @@
         logger_->debug("[ice:{}] Creating IceTransport session", fmt::ptr(this));
     skId_ = skIceCreated.fetch_add(1, std::memory_order_relaxed) + 1;
     skBorn_ = std::chrono::steady_clock::now();
-    SK_ICE("create #%u alive=%d (created=%u destroyed=%u)",
-           skId_, skIceAlive.fetch_add(1, std::memory_order_relaxed) + 1,
+    // name is "Device <id>" from IceTransportFactory's callers, i.e. the peer this session is for.
+    // It is what ties a transport id to a device, which is how a long-lived transport gets
+    // identified.
+    //
+    // This used to claim it was "safe to read here ... the daemon leaves opendht's logger null in
+    // release". That was false twice over: Manager holds ice_tf_ built with Logger::dhtLogger(),
+    // which is never null, and createUTransport falls back to the factory's own logger precisely
+    // when the per-connection one is null. So a logger always existed, the init list always moved,
+    // and this field was ALWAYS empty — the measured create lines print `peer=` with nothing after
+    // it. The move is now a copy; see the constructor's init list.
+    SK_ICE("create #%u peer=%s alive=%d (created=%u destroyed=%u)",
+           skId_, name.c_str(), skIceAlive.fetch_add(1, std::memory_order_relaxed) + 1,
            skIceCreated.load(), skIceDestroyed.load());
 }
 
 IceTransport::Impl::~Impl()
 {
-    SK_ICE("destroy #%u alive=%d lifetime=%llds (created=%u destroyed=%u)",
-           skId_, skIceAlive.fetch_sub(1, std::memory_order_relaxed) - 1,
+    // Final ICE state on the way out: of everything created, how much ever actually connected?
+    // RUNNING means it succeeded, FAILED lost, anything else means it was torn down mid-flight.
+    //
+    // This line used to print the raw integer with the legend "4=RUNNING, 5=FAILED" — which is off
+    // by one against pjnath/include/pjnath/ice_strans.h (RUNNING is 5, FAILED is 6), so the census
+    // it fed read exactly backwards: transports counted as connected were still in NEGO, and those
+    // written off as failed had in fact reached RUNNING. Print the name; never the number.
+    SK_ICE("destroy #%u state=%s alive=%d lifetime=%llds (created=%u destroyed=%u)",
+           skId_, icest_ ? skIceStateName(pj_ice_strans_get_state(icest_)) : "none",
+           skIceAlive.fetch_sub(1, std::memory_order_relaxed) - 1,
            (long long) std::chrono::duration_cast<std::chrono::seconds>(
                std::chrono::steady_clock::now() - skBorn_).count(),
            skIceCreated.load(), skIceDestroyed.fetch_add(1, std::memory_order_relaxed) + 1);
--- a/src/connectionmanager.cpp
+++ b/src/connectionmanager.cpp
@@ -34,6 +34,17 @@
 #include <random>
 #include <set>
 
+// SK-ICEDIAG (2026-07-30): which side initiates an ICE session. The transport census showed ~5.4
+// creations a minute sustained (bursting to 15) with almost all sessions failing on a CGNAT
+// cellular link; this says whether WE are retrying outbound or peers are retrying at US, which
+// are opposite problems with opposite fixes.
+#ifdef __ANDROID__
+#include <android/log.h>
+#define SK_CM(...) __android_log_print(ANDROID_LOG_ERROR, "SK-ICEDIAG", __VA_ARGS__)
+#else
+#define SK_CM(...) do {} while (0)
+#endif
+
 namespace dhtnet {
 static constexpr std::chrono::seconds DHT_MSG_TIMEOUT {30};
 static constexpr dht::Value::Id ID_MAX_VAL = 9007199254740992;
@@ -1129,6 +1140,7 @@
         ice_config.master = false;
         ice_config.streamsCount = 1;
         ice_config.compCountPerStream = 1;
+        SK_CM("initiate %s device=%s", "connect-out", std::string(deviceId.to_view()).c_str());
         info->ice_ = sthis->config_->factory->createUTransport(fmt::format("Device {}", deviceId.to_view()),
                                                                sthis->config_->logger);
         if (!info->ice_) {
@@ -1689,6 +1701,7 @@
         if (shared->config_->logger)
             shared->config_->logger->debug("[device {}] Accepting connection", deviceId);
         std::unique_lock lk {info->mutex_};
+        SK_CM("initiate %s device=%s", "answer-in", std::string(deviceId.to_view()).c_str());
         info->ice_ = shared->config_->factory->createUTransport(fmt::format("Device {}", deviceId.to_view()),
                                                                 shared->config_->logger);
         if (not info->ice_) {
