/ring-daemon
env.sh
obj/
android-toolchain-*/
.idea/
.DS_Store
.gradle
**/.settings
config.mak
jami-android/captures/
.cxx
.vscode/settings.json

# Claude Code — per-user settings (Claude Code creates these)
.claude/settings.local.json
.claude/*.local.json
.claude/*.*~

# Claude Code — transient session files (if Claude Code ever drops them in-tree)
.claude/sessions/
.claude/.cache/
