<?xml version="1.0" encoding="utf-8"?>
<!-- shiroikuma: yellow ring; filled yellow when active (icon flips to black via
     color/shiroikuma_call_icon). -->
<selector xmlns:android="http://schemas.android.com/apk/res/android"
    android:enterFadeDuration="@android:integer/config_shortAnimTime"
    android:exitFadeDuration="@android:integer/config_shortAnimTime">
    <item android:state_checked="true">
        <shape android:innerRadius="0dp" android:shape="ring" android:thicknessRatio="2" android:useLevel="false">
            <solid android:color="@color/shiroikuma_call_yellow" />
            <stroke android:width="2dp" android:color="@color/shiroikuma_call_yellow" />
        </shape>
    </item>
    <item>
        <shape android:innerRadius="0dp" android:shape="ring" android:thicknessRatio="2" android:useLevel="false">
            <solid android:color="@android:color/transparent" />
            <stroke android:width="2dp" android:color="@color/shiroikuma_call_yellow" />
        </shape>
    </item>
</selector>
